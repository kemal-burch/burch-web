angular
  .module("myApp")
  .controller("indexController", [
    "$scope",
    "$http",
    "$state",
    "$cookies",
    "$window",
    function($scope, $http, $state, $cookies, $window) {}
  ]);
